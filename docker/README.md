# Docker Resources

This directory contains configuration files and certificates for running Orbit under Docker.

- `letsencrypt/`: Autogenerated SSL certificates directory (created by Traefik if SSL resolver is active).
